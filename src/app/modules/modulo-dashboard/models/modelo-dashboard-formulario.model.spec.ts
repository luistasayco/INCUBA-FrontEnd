import { ModeloDashboardFormulario } from './modelo-dashboard-formulario.model';

describe('ModeloDashboardFormulario', () => {
  it('should create an instance', () => {
    expect(new ModeloDashboardFormulario()).toBeTruthy();
  });
});
